import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  calendarButton: {
    margin: 20,
    alignSelf: 'center',
  },
  calendarText: {
    fontSize: 20,
  },
});

export default styles;
