import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  chartpieButton: {
    margin: 20,
    alignSelf: 'center',
  },
  chartpieIcon: {
    fontSize: 60,
  },
});

export default styles;
